import { SwagHighlightPipe } from './swag-highlight.pipe';

describe('SwagHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new SwagHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
